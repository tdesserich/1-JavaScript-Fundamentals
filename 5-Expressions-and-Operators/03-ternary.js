// var x = 6;

// (x > 0) ? 'yay' : 'nay'

// if (x > 0) {
//     console.log('yay');
// } else {
//     console.log('nay');
// }



// var age = 20;

// var ternAge = (age >=25) ? "Yay! You can rent a car!" : (age >=21) ? "Yay! You can drink!" : (age>=18) ? "Yay! You can vote" : "Sorry. You are too young to do anything fun."
// console.log(ternAge);


var x = "Tracy";

var ternx = (typeof x === 'string') ? "String cheese" : (typeof x === 'number') ? "numbers are cool" : (typeof x === 'boolean') ? "truthiness" : 'nothing'

console.log(ternx);

